{
  "name": "@heaven-pass/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic helpers (money, dates, pagination) shared by apps/api and apps/mobile",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@heaven-pass/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
